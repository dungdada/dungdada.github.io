# dungdada.github.io

정다원 (Dawon Jung) — AI Agent / LLM Application Developer 포트폴리오.

이 저장소에는 **빌드된 정적 산출물만** 있습니다. 소스는 `~/Desktop/Project/portfolio` (React + Vite + TypeScript).

```bash
npm install
npm run dev      # 로컬 개발 서버
npm run build    # dist/ 생성 → 이 저장소로 복사
```

- 한국어 / 영어 전환 지원
- 라우팅은 HashRouter (GitHub Pages 의 404 회피)
- 사이트의 모든 수치와 인용은 실제 저장소·실행 로그·산출 파일에서 확인한 값만 사용
